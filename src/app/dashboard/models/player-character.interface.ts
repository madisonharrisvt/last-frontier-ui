import { Identity } from "./identity.interface";

export class PlayerCharacter {
    playerId: number;
    characterId: number;
    firstName: string;
    lastName: string;
    characterName: string;
}